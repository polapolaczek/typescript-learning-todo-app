import React from "react";
import Button, { ButtonProps } from "../Button";

const DoneButton = (props: ButtonProps) => {
    return <Button type="button" onClick={() => {}} {...props} />;
};
export default DoneButton;
